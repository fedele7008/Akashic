// Tailwind v3 + autoprefixer pipeline. Standard for Next.js setups
// that haven't moved to Tailwind v4's CSS-only config yet.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
