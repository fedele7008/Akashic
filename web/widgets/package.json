{
  "name": "@akashic/widgets",
  "version": "0.0.1",
  "private": true,
  "description": "Embeddable Web Component widgets for Akashic identity. Hosted on api.<tenant>/widgets/akashic.js — tenants integrate via a single <script> tag plus a custom element.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
